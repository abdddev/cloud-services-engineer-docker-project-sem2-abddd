# Адрес API бэкенда — вшивается в JS-бандл на этапе сборки фронтенда.
# Браузер обращается к этому адресу напрямую.
# Скопируйте файл в .env и при необходимости измените значение:
#   cp .env.example .env

VUE_APP_API_URL=http://localhost:8081
